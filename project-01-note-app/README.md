A simple notes taking console app.

1. Setup: npm install

2. run program:
    node main.js [command] --[option]

3. commands:
    command: add, remove, list, read
    option: --title=[value], --body=[value]
